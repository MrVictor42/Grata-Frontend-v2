import React, { Component } from 'react';
import { Button } from 'antd';

export class ResultQuiz extends Component {
    render() {
        return (
            <span>
                <Button> Resultado do Questionário </Button>
            </span>
        )
    }
}

export default ResultQuiz;